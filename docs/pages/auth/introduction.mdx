# Authentication

This package provides a way to authenticate with twitch as a user or an app.
It automatically refreshes your `user` access token and fetches a new `app` access token on expiration.

### Links
- [Twitch authentication documentation](https://dev.twitch.tv/docs/authentication)